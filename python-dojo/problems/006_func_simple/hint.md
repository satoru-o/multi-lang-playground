# ヒント

- 関数は `def 関数名(引数):` で定義し、`return 値` で結果を返します。
- `print` ではなく `return` を使うことに注意してください（今回は出力ではなく戻り値をチェックします）。
- 形:
  ```python
  def is_even(n):
      if n % 2 == 0:
          return True
      else:
          return False
  ```
- もっと短く `return n % 2 == 0` の1行でも書けます。
