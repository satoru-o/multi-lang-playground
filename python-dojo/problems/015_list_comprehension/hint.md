# ヒント

- この問題は「リスト内包表記」の練習です。for文でも正解にはなりますが、ぜひ内包表記で書いてみてください。
- 内包表記の形: `[式 for 変数 in リスト if 条件]`
- 形:
  ```python
  def multiples_of_three(numbers):
      return [n for n in numbers if n % 3 == 0]
  ```
