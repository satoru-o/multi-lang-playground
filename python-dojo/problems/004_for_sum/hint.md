# ヒント

- `range(1, 11)` は 1, 2, ..., 10 を順に生成します（11は含まれません）。
- 合計を貯めていく変数を先に `total = 0` のように用意しておきます。
- 形:
  ```python
  total = 0
  for i in range(1, 11):
      total = total + i
  print(total)
  ```
